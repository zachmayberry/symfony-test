{% extends "@FOSUser/layout.html.twig" %}

{% block fos_user_content %}

    {% if app.user %}
        You are already logged in as "{{ app.user.username }}". If this is not you, please <a href="{{ path('fos_user_security_logout') }}">change user</a>.
    {% else %}
        {{ include('@FOSUser/Security/login_content.html.twig') }}
    {% endif %}

{% endblock fos_user_content %}
